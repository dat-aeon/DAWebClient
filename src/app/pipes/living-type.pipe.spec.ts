import { LivingTypePipe } from './living-type.pipe';

describe('LivingTypePipe', () => {
  it('create an instance', () => {
    const pipe = new LivingTypePipe();
    expect(pipe).toBeTruthy();
  });
});
