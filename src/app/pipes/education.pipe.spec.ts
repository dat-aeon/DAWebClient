import { EducationPipe } from "./education.pipe";


describe('CityPiEducationPipepe', () => {
  it('create an instance', () => {
    const pipe = new EducationPipe();
    expect(pipe).toBeTruthy();
  });
});
