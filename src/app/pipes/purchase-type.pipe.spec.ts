import { PurchaseTypePipe } from "./purchase-type.pipe";


describe('PurchaseTypePipe', () => {
  it('create an instance', () => {
    const pipe = new PurchaseTypePipe();
    expect(pipe).toBeTruthy();
  });
});
