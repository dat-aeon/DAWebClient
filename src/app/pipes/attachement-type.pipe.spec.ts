import { AttachementTypePipe } from './attachement-type.pipe';

describe('AttachementTypePipe', () => {
  it('create an instance', () => {
    const pipe = new AttachementTypePipe();
    expect(pipe).toBeTruthy();
  });
});
