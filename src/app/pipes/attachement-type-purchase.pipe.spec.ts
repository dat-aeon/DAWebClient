import { AttachementTypePurchasePipe } from './attachement-type-purchase.pipe';

describe('AttachementTypePurchasePipe', () => {
  it('create an instance', () => {
    const pipe = new AttachementTypePurchasePipe();
    expect(pipe).toBeTruthy();
  });
});
